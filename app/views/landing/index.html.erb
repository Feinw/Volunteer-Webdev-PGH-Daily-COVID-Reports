<h1>data</h1>

<%= column_chart( [{name: "with", data: @w_covid}, {name: "pui", data: @pui}, {name: "discharged", data: @discharged}, {name: "expired", data: @expired}] )%>


<div class="container">
    <table class="table">
    <tbody>
        <% @rows.each do |row| %>
            <tr>
                <% row.each do |col| %>
                    <td><%= col %></td>
                <% end %>
            </tr>
        <% end %>
    </tbody>
    </table>
</div>